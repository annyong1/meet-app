module.exports = {
  launch: {
    headless: true, // set to false if you want to see the browser during testing
    product: "chrome", // specify product if needed, e.g. chrome
  },
  browserContext: "default",
};
